/**
 * Keep upstream MDX tags compatible with the single-product documentation.
 * Feature availability is described by the page, never by an edition badge.
 */
export default function FeatureState() {
  return null;
}

export function Pro() {
  return null;
}

export function Enterprise() {
  return null;
}
