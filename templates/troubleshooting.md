# Troubleshooting <feature>

<!--
Troubleshooting page. One H2 per symptom, phrased the way the reader sees it.
For an error message, use the message itself as the heading, prefixed with `Error:`,
and put the message in backticks.
Each entry: symptom, then cause, then fix. Keep "workaround" for temporary fixes only.
-->

<a id="error-slug"></a>

## Error: `<the exact message the user sees>`

**Cause.** <Why this happens, one or two sentences.>

**Fix.**

1. <Action.>
2. <Action.>

<a id="symptom-slug"></a>

## <Symptom phrased as the user would describe it>

**Cause.** <…>

**Fix.** <…>
